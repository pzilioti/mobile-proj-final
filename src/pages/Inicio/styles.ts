import { Dimensions, StyleSheet } from 'react-native';

export default StyleSheet.create({

    button: {
        alignItems: 'center',
        marginTop: 10,
        padding: 10,
    },

    buttonText: {
        fontSize: 22
    }


});